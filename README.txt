SER 421 - Module 3 Lab Submission:
Student: Nina Mason
Date: 9/7/2025

-Module 3 Activity 1:
 - I fulfilled all three requirements for this activity: if the user enters 'idk' into any of the text inputs the required String appears at the bottom of screen; if the user provides 3 answers (> length 0) none of which are idk then the other text output appears; and finally when 'Go again' button is clicked the 3rd question switches between the two options.
 - HTML file with no build tools and no CSS included.

-Module 3 Activity 2:
 - I fulfilled all six requirements: when user is not on the last question then clicking 'OK' takes them to the next question; the first line will state the user's current score; second line says 'Question #' with the current question number followed by the question; the available choices for the user's answer will be rendered as a vertical line of radio buttons; styled the first 2 lines and the choices correctly; and when the user completes the questions the last screen shows the correct message and formatting.
 - Styling is done in a <style> block, given code is used as starter, contains <script>, <template>, <style>, and all is implemented within a single SFC.

-Module 3 Activity 3:
 - I fulfilled all five requirements: includes the components HelloWorld, Balance, and Currency from sample code given; App.vue contains my name in the root component and display it in HelloWorld.vue; Balance has a slider bound to the amount and ranges from 5-100, incrementing by 5s; Balance will no longer subtract if the amount > balance; the balance denomination in the Balance component matches the denomination chosen in the 'Convert From:' field of Currency; and the 'Enter Amount' field on Currency shows a default value equal to the Balance amount.
 - HelloWorld, Balance, and Currency are separate SFCs and the project was created using 'npm init vue@latest'
 - I tested this program by using command prompt to run 'npm run dev' and then copy/pasting the localhost into my browser. Since I removed node_modules and package-lock.json, this might require the 'npm install' command and then the 'npm run dev' command to run the program now.
	